[project]
name = "fm-bot-control"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.109.0",
  "uvicorn>=0.27.0",
  "python-dotenv>=1.0.0",
  "requests>=2.31.0",
  "pydantic>=2.0.0",
  "openai>=1.40.0",
]

[tool.vercel]
entrypoint = "app:app"
